<IfModule mod_rewrite.c>
    RewriteEngine On

    # Let Apache serve real files/directories directly and send everything
    # else through Flight's front controller.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
